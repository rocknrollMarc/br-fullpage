'use strict';

module.exports = {
    example: {
        options:{
            base: 'example',
            port: 4000,
            hostname: 'localhost',
            open: true,
            livereload: true
        }
    }
};