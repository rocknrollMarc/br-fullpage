'use strict';

module.exports = {
    example: {
        options:{
            base: 'example',
            port: 8080,
            hostname: 'localhost',
            open: true,
            livereload: true
        }
    }
};