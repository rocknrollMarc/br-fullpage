'use strict';

module.exports = {
    example: {
        options:{
            base: 'example',
            port: 4000,
            hostname: '*',
            open: true,
            livereload: true
        }
    }
};